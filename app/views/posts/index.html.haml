%h1 Forum - Ask anything, help anyone.

- @posts.each do |post|
  %h2= link_to post.title, post
  %p
    Posted
    = time_ago_in_words(post.created_at)
    ago
    by
    = post.user.name

= link_to "New Post", new_post_path
